Рекурсия - это решение задачи с использованием аналогичной(-ых) подзадачи(-ч).

Подзадача должна быть проще исходной задачи, т.е. приближать рекурсию к конечному случаю.

Нельзя допускать переполнения стека.

каждый вызов функции порождает пространство имён этой функции.